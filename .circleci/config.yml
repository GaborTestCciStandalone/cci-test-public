version: 2.1

jobs:
 hello-world2:
    docker:
      - image: cimg/node:25.7.0 # the primary container, where your job's commands are run
    steps:
      - checkout # check out the code in the project directory
      - run: echo "hell there123345aaaaaa" # run the `echo` command
      - run: echo "master kenobi!1234534aaaaa"

workflows:
  test-workflow:
    jobs:
      - hello-world2
